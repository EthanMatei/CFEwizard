package cfe.calc;

import cfe.model.CfeResults;

/**
 * WORK IN PROGRESS
 * 
 * Goal: separate out the pipeline process so that it can be used with multiple interfaces.
 * 
 * @author Jim Mullen
 *
 */
public class Pipeline {
    
    public CfeResults cacluateDiscoveryCohort(String pheneTable, String phene, double lowCutoff, double highCutoff) {
        CfeResults results = new CfeResults();
        return results;
    }
}
